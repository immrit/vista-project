# راهنمای تست قابلیت اشتراک‌گذاری تصویری پست‌ها

## نحوه تست

### ۱. اجرای برنامه

```bash
flutter run
```

### ۲. ورود به برنامه و رفتن به پست‌ها

- وارد حساب کاربری شوید
- به صفحه پست‌ها (Public Posts) بروید
- روی یکی از پست‌ها کلیک کنید

### ۳. تست قابلیت اشتراک‌گذاری

- روی دکمه اشتراک‌گذاری (آیکون share) کلیک کنید
- گزینه "اشتراک‌گذاری تصویری" را انتخاب کنید

### ۴. استفاده از گزینه‌های تست

#### پیش‌نمایش تصویر

- دکمه **پیش‌نمایش تصویر** رو بزنید
- تصویر پست در یک پنجره بزرگ نمایش داده می‌شود
- می‌توانید تصویر رو زوم کنید و بررسی کنید

#### ذخیره در گالری

- دکمه **ذخیره در گالری** رو بزنید
- تصویر در گالری دستگاه ذخیره می‌شود
- به گالری دستگاه بروید و تصویر رو چک کنید

#### ذخیره در Documents

- دکمه **ذخیره در Documents** رو بزنید
- تصویر در پوشه Documents اپ ذخیره می‌شود
- مسیر فایل نمایش داده می‌شود
- می‌توانید از File Manager یا ADB برای دسترسی به فایل استفاده کنید

## بررسی کیفیت تصویر

### محتوای تصویر باید شامل

- ✅ لوگو Vista
- ✅ آواتار و نام کاربری نویسنده پست
- ✅ تاریخ پست (به صورت شمسی)
- ✅ محتوای متنی پست
- ✅ تصویر پست (در صورت وجود)
- ✅ نشان‌های تأیید (تیک آبی/طلایی/مشکی)
- ✅ تعداد لایک و کامنت
- ✅ فونت فارسی خوانا

### نکات مهم برای بررسی

- اندازه تصویر باید مناسب برای اشتراک‌گذاری باشد (حدود 1080x1920)
- متن فارسی باید به درستی نمایش داده شود
- رنگ‌ها و کنتراست باید مناسب باشد
- لوگو و برندینگ باید واضح باشد

## دسترسی به فایل‌های تست

### از طریق ADB

```bash
# لیست فایل‌های Documents
adb shell ls -la /data/data/com.example.vista/app_flutter/

# کپی فایل به کامپیوتر
adb pull "/data/data/com.example.vista/app_flutter/vista_post_xxx.png" .
```

### از طریق Device File Explorer (Android Studio)

1. Android Studio رو باز کنید
2. View → Tool Windows → Device File Explorer
3. به مسیر بروید: `/data/data/com.example.vista/app_flutter/`
4. فایل‌های PNG رو دانلود کنید

## اشتراک‌گذاری واقعی

بعد از اطمینان از کیفیت تصویر، می‌توانید از دکمه‌های اصلی استفاده کنید:

- **اینستاگرام**: اشتراک‌گذاری به استوری اینستاگرام
- **واتساپ**: اشتراک‌گذاری به واتساپ
- **سایر اپ‌ها**: اشتراک‌گذاری به سایر برنامه‌ها

## نکات مهم

1. **دسترسی‌ها**: مطمئن شوید که دسترسی ذخیره‌سازی به اپ داده شده
2. **حافظه**: فضای کافی برای ذخیره تصاویر داشته باشید
3. **کیفیت**: تصاویر باید برای شبکه‌های اجتماعی بهینه باشند
4. **اندازه**: فایل‌های خیلی بزرگ ممکنه مشکل ایجاد کنند

## عیب‌یابی

### اگر تصویر تولید نشد

- بررسی کنید دسترسی ذخیره‌سازی داده شده باشد
- فضای کافی روی دستگاه داشته باشید
- برنامه رو ری‌استارت کنید

### اگر تصویر کیفیت خوبی ندارد

- تنظیمات فونت و اندازه‌ها رو بررسی کنید
- رزولوشن تصویر رو چک کنید
- رنگ‌ها و کنتراست رو تنظیم کنید

### اگر اشتراک‌گذاری کار نکرد

- مطمئن شوید اپ مقصد نصب باشد
- دسترسی‌های لازم رو چک کنید
- برنامه‌های مقصد رو به‌روز کنید

