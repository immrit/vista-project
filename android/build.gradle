buildscript {
    ext.kotlin_version = '1.9.20'
    repositories {
        google()
        mavenCentral() // added repository
    }
        dependencies {
        classpath 'com.android.tools.build:gradle:7.3.0' // یا نسخه Android Gradle Plugin شما
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
    }
}

allprojects {
    repositories {
        google()
        mavenCentral() // added repository
        // Add this new repository
        // maven { url "https://repo1.maven.org/maven2/" }
        // maven { url "https://jitpack.io" }
    }
    configurations.all {
        resolutionStrategy {
            force 'androidx.exifinterface:exifinterface:1.3.5' // force a stable version
        }
    }
}

rootProject.buildDir = "../build"
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(":app")
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
